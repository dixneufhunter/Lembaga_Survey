@model IEnumerable<ConsumeWebAPI.Models.KabupatenViewModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ID_KAB)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.KODE_KABUPATEN)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NAMA_KABUPATEN)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ID_KAB)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.KODE_KABUPATEN)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NAMA_KABUPATEN)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.ID_KAB }) |
                @Html.ActionLink("Details", "Details", new { id = item.ID_KAB }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.ID_KAB })

            </td>
        </tr>
}
    </tbody>
</table>
